<?xml version="1.0" encoding="utf-8"?>
<!--
    feedbackGeneric = we are NOT a screen reader/TTS service.
    Using feedbackSpoken caused Android to classify this as a screen reader,
    which shows misleading UI and can restrict the service on some OEMs.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeViewTextChanged|typeAnnouncement"
    android:accessibilityFlags="flagDefault"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:description="@string/accessibility_description"
    android:notificationTimeout="100"
    android:canRetrieveWindowContent="true" />
